<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="0dp"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="0dp"
    tools:context="com.squadpay.SquadPayTabs$PlaceholderFragment">
<!-- creating a divide between items in a recycler view by using the same grey background color as the parent
    layout. Doing dividers this way can cause overdraw for large amounts of data -->
    <android.support.v7.widget.RecyclerView
        android:id="@+id/tabs_recycler_view"
        android:background="@color/light_grey"
        android:paddingTop="32dp"
        android:clipToPadding="false"
        android:scrollbars="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

</RelativeLayout>
